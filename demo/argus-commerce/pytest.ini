[pytest]
# The demo services are imported as ``services.*`` from the repository root, and
# the checkout path exercises async code. Both are ordinary Python-app settings;
# without them the demo's own test suite cannot run at all.
asyncio_mode = auto
pythonpath = .
testpaths = tests
